ignores: [
  '@nestjs/schematics', # По умолчанию для NestJS
  'ts-loader',          # Используется системой сборки приложения. Не импортируется в явном виде
  'tsconfig-paths',     # При вызове считывает пути из tsconfig.json и преобразует пути к физическим файлам. Не импортируется в явном виде
  'pg',                 # Драйвер для typeorm. Не импортируется в явном виде
  'source-map-support',
  'crypto'              # Используется для генерации uuid
]
skip-missing: false 
